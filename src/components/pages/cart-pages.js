import React from 'react';
import CartTable from '../cart-table';

const CartPages = () => {
    return (
        <div className="cart">
            <CartTable/>
        </div>
    )
}

export default CartPages;
